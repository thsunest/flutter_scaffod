import 'package:flutter/material.dart';

class  TeamLine extends StatelessWidget {
  const  TeamLine({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}